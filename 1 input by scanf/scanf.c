#include <stdio.h>
int main(){
    int age;

    printf("Please enter your age: ");
    scanf("%d",&age);
    // where %d uses to get the integer values and %c for character

    printf("Your age is: %d",age);
    return 0;
}